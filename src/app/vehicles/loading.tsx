import { VehiclesSkeleton } from './components/VehiclesSkeleton';

// Loading state for the vehicles pages. Displays skeleton placeholders.
export default function Loading() {
  return <VehiclesSkeleton />;
}
